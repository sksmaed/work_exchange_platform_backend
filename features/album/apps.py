from django.apps import AppConfig


class AlbumConfig(AppConfig):
    """Configuration for the album feature app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "features.album"
