from django.apps import AppConfig


class HelperResumeConfig(AppConfig):
    """Configuration for the Helper Resume app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "features.helper.resume"
    label = "helper_resume"
