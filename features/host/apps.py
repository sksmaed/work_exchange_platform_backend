from django.apps import AppConfig


class HostConfig(AppConfig):
    """Configuration for the host feature app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "features.host"
